/*
JTSK-320111
Problem a5.p10.c
Jonathan Rittmayer
jo.rittmayer@jacobs-university.de
*/

/*
Write a function
  void proddivpowinv(float a, float b, float *prod, float *div,float *pwr, float *invb)

that computes and “returns” by reference
  the product,
  the division of the two floats
  the resul t of ab and 1/b.

Also write a simple test program to check that your function works correctly (by printing on the
screen the results from the main() function).
*/

#include <string.h>
#include <stdio.h>

/*
@brief This is a function that computes and “returns” by reference
  the product,
  the division of the two floats
  the resul t of ab and 1/b.

@param *str: The string used to count consonants
*/
void proddivpowinv(float a, float b, float *prod, float *div,float *pwr, float *invb);

  int main () {
    return 0;
}
